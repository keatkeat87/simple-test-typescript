{
  "name": "simple-test-typescript",
  "packageManager": "yarn@3.2.1",
  "devDependencies": {
    "@yarnpkg/esbuild-plugin-pnp": "^3.0.0-rc.7",
    "esbuild": "^0.14.43",
    "typescript": "^4.7.3"
  },
  "scripts": {
    "start": "yarn node esbuild.js",
    "esbuild": "esbuild index.ts --bundle --outfile=bundle.js --watch"
  }
}
